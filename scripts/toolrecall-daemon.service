[Unit]
Description=ToolRecall Daemon — Tool-Output Cache + MCP Multiplexer
Documentation=https://github.com/whiskybeer/toolrecall
After=network.target

[Service]
Type=simple
ExecStart=python3 -m toolrecall daemon --foreground
Restart=on-failure
RestartSec=5
# Allow more retries — stale-socket takeover can take a few attempts
# before the old instance fully releases the port.
StartLimitInterval=120s
StartLimitBurst=5
StandardOutput=journal
StandardError=journal

# Hardening
PrivateTmp=true
NoNewPrivileges=true
ProtectHome=soft
ProtectSystem=full

[Install]
WantedBy=multi-user.target
