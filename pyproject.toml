[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "toolrecall"
version = "0.1.0"
description = "Universal Tool Output Cache for LLM Agents"
requires-python = ">=3.10"

[project.scripts]
toolrecall = "toolrecall.cli:main"
